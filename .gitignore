# Your own server config -- holds admin identifiers
server_config.lua

# Working notes, not shipped and not published
AGENTS.md

# Dependencies
node_modules/

# Build caches. web/dist is NOT ignored -- fxmanifest ships it in files{},
# so the built UI has to be committed for the resource to run.
.vite/
*.tsbuildinfo

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor and OS
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini
